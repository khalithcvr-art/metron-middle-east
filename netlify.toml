[build]
  command = "npm ci --include=dev --legacy-peer-deps && npm run build"
  publish = "dist/public"
  environment = { NODE_VERSION = "20" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
