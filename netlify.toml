[build]
  command = "npm install && npm run build"
  publish = "dist/public"
  environment = { NODE_VERSION = "20", NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
