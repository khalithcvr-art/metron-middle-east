[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist/public"
  environment = { NODE_VERSION = "20", NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
