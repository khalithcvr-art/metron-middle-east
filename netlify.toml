[build]
  command = "npm ci --legacy-peer-deps && node --version && npm --version && npm run build"
  publish = "dist/public"
  environment = { NODE_VERSION = "20", NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
